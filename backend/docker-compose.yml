version: "3.9"
services:
  backend:
    build:
      context: ./backend
    container_name: projectbill-backend
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./backend:/app
